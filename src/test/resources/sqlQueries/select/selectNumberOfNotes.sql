SELECT COUNT(*) FROM NOTE WHERE (REFERENCE_ID IN (SELECT CONCAT('Referral', ID) FROM REFERRAL r WHERE r.PARTICIPANT_ID = (SELECT ID FROM PARTICIPANT p WHERE p.FIRST_NAME = '%1$s' and p.LAST_NAME = '%2$s')))